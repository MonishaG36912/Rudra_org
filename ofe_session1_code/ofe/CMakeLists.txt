cmake_minimum_required(VERSION 3.20)
project(OrderFlowEngine VERSION 1.0.0 LANGUAGES CXX)

# ── C++ standard ──────────────────────────────────────────────────────────────
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# ── Build type defaults ────────────────────────────────────────────────────────
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE Release)
endif()

# ── Compiler flags ────────────────────────────────────────────────────────────
if(MSVC)
    add_compile_options(
        /W4 /WX           # All warnings as errors
        /O2               # Optimise for speed
        /EHsc             # Standard exception handling
        $<$<CONFIG:Debug>:/DDEBUG /Od /Zi>
        $<$<CONFIG:Release>:/DNDEBUG /O2 /GL>  # Whole program optimisation
    )
    add_link_options(
        $<$<CONFIG:Release>:/LTCG /OPT:REF /OPT:ICF>  # Strip unused code
    )
else()
    # Base flags applied to all targets (including FetchContent deps)
    add_compile_options(-Wall -Wextra -Wpedantic -march=native)

    # Per-config flags scoped to OFE targets only (avoids polluting deps)
    set(OFE_COMPILE_FLAGS
        $<$<CONFIG:Debug>:-O0>
        $<$<CONFIG:Debug>:-g>
        $<$<CONFIG:Debug>:-fsanitize=address,undefined>
        $<$<CONFIG:Release>:-O3>
        $<$<CONFIG:Release>:-DNDEBUG>
        $<$<CONFIG:Release>:-flto>
    )
    set(OFE_LINK_FLAGS
        $<$<CONFIG:Debug>:-fsanitize=address,undefined>
        $<$<CONFIG:Release>:-flto>
        $<$<CONFIG:Release>:-Wl,--strip-all>
    )
endif()

# ── Include directories ───────────────────────────────────────────────────────
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/include)

# ── External dependencies ─────────────────────────────────────────────────────
include(FetchContent)

# Google Test — unit testing
FetchContent_Declare(
    googletest
    GIT_REPOSITORY https://github.com/google/googletest.git
    GIT_TAG        v1.14.0
)
FetchContent_MakeAvailable(googletest)

# yaml-cpp — configuration file parsing
FetchContent_Declare(
    yaml-cpp
    GIT_REPOSITORY https://github.com/jbeder/yaml-cpp.git
    GIT_TAG        0.8.0
)
FetchContent_MakeAvailable(yaml-cpp)

# nlohmann/json — JSON parsing for Coinbase adapter (header-only)
FetchContent_Declare(
    nlohmann_json
    GIT_REPOSITORY https://github.com/nlohmann/json.git
    GIT_TAG        v3.11.3
)
FetchContent_MakeAvailable(nlohmann_json)

# IXWebSocket — async WebSocket+TLS client for Coinbase adapter
# Requires OpenSSL (apt install libssl-dev) for wss:// support
find_package(OpenSSL REQUIRED)
set(USE_TLS        ON CACHE BOOL "IXWebSocket: enable TLS"     FORCE)
set(USE_OPEN_SSL   ON CACHE BOOL "IXWebSocket: use OpenSSL"    FORCE)
FetchContent_Declare(
    ixwebsocket
    GIT_REPOSITORY https://github.com/machinezone/IXWebSocket.git
    GIT_TAG        v11.4.5
)
FetchContent_MakeAvailable(ixwebsocket)

# ── Source files ──────────────────────────────────────────────────────────────
# (Populated as AGT-03 / AGT-04 / AGT-05 generate implementation files)

set(OFE_CORE_SOURCES
    src/core/tick_record.cpp
    src/core/ring_buffer.cpp
    src/core/tick_router.cpp
    src/core/lee_ready.cpp
    src/core/bar_types.cpp
    src/core/bar_engine.cpp
    src/core/symbol_worker.cpp
    src/core/event_bus_redis.cpp
)

set(OFE_ANALYTICS_SOURCES
    src/analytics/delta_engine.cpp
    src/analytics/imbalance_detector.cpp
    src/analytics/volume_profile.cpp
    src/analytics/vwap_engine.cpp
    src/analytics/signal_detector.cpp
)

set(OFE_FEED_SOURCES
    src/feed/coinbase_adapter.cpp      # AGT-05b: free Coinbase WebSocket feed (DONE)
    # src/feed/iqfeed_adapter.cpp      # AGT-05: IQFeed TCP (TODO)
    # src/feed/kinetick_cpp_receiver.cpp
    # src/feed/esignal_adapter.cpp
    # src/feed/feed_manager.cpp
    # src/feed/adapter_factory.cpp
)

set(OFE_LICENSE_SOURCES
    # AGT-09 outputs:
    # src/license/license_engine.cpp
    # src/license/hw_fingerprint.cpp
)

# ── Main engine library ───────────────────────────────────────────────────────
add_library(ofe_engine SHARED
    ${OFE_CORE_SOURCES}
    ${OFE_ANALYTICS_SOURCES}
    ${OFE_FEED_SOURCES}
    ${OFE_LICENSE_SOURCES}
    src/util/logger.cpp
)

target_include_directories(ofe_engine PUBLIC
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>
)

target_compile_options(ofe_engine PRIVATE ${OFE_COMPILE_FLAGS})
target_link_options(ofe_engine PRIVATE ${OFE_LINK_FLAGS})
target_link_libraries(ofe_engine PRIVATE
    yaml-cpp
    nlohmann_json::nlohmann_json
    ixwebsocket
    OpenSSL::SSL
    OpenSSL::Crypto
    pthread  # Linux thread support
)

# ── Engine executable ─────────────────────────────────────────────────────────
# add_executable(ofe_server
#     # src/main.cpp   (added when generated by AGT-03)
# )
# target_link_libraries(ofe_server PRIVATE ofe_engine)

# ── Unit tests ────────────────────────────────────────────────────────────────
# AGT-10 Test Agent will populate these:
enable_testing()

add_executable(ofe_tests
    test/test_tick_record.cpp
    test/test_ring_buffer.cpp
    test/test_lee_ready.cpp
    test/test_coinbase_jwt.cpp         # JWT ES256 offline signing tests (no network required)
    test/test_bar_engine.cpp           # AGT-10: bar construction
    test/test_delta_engine.cpp         # AGT-10: all 5 delta formulas
    test/test_imbalance_detector.cpp   # AGT-10: single/stacked/absorption/trapped
    test/test_volume_profile.cpp       # AGT-10: POC, VA two-level, shape D/P/b
    test/test_vwap_engine.cpp          # AGT-10: VWAP incremental, bands, signals
    test/test_signal_detector.cpp      # AGT-10: COT, Ratio, SinglePrint, ZeroPrint
    # test/test_pulse.cpp
    # test/test_turns.cpp
    # test/test_license_engine.cpp
)
target_compile_options(ofe_tests PRIVATE ${OFE_COMPILE_FLAGS})
target_link_options(ofe_tests PRIVATE ${OFE_LINK_FLAGS})
target_link_libraries(ofe_tests PRIVATE
    ofe_engine
    OpenSSL::SSL
    OpenSSL::Crypto
    GTest::gtest_main
)
include(GoogleTest)
gtest_discover_tests(ofe_tests)

# ── Live integration tests (require network — NOT part of ofe_tests suite) ────
# Build: cmake --build build --target coinbase_live_test
# Run:   ./build/coinbase_live_test
# Auth:  COINBASE_KEY_NAME=... COINBASE_PRIVATE_KEY_FILE=... ./build/coinbase_live_test
add_executable(coinbase_live_test
    tools/coinbase_live_test.cpp
)
target_compile_options(coinbase_live_test PRIVATE ${OFE_COMPILE_FLAGS})
target_link_options(coinbase_live_test PRIVATE ${OFE_LINK_FLAGS})
target_link_libraries(coinbase_live_test PRIVATE ofe_engine)

# ── Live BTC/USD analytics (§AGT-05c) ────────────────────────────────────────
# Wires Coinbase public feed → BarEngine(60s) + DeltaEngine + VwapEngine.
# No credentials required.
# Build: cmake --build build --target btc_live_analytics
# Run:   ./build/btc_live_analytics
add_executable(btc_live_analytics
    tools/btc_live_analytics.cpp
)
target_compile_options(btc_live_analytics PRIVATE ${OFE_COMPILE_FLAGS})
target_link_options(btc_live_analytics PRIVATE ${OFE_LINK_FLAGS})
target_link_libraries(btc_live_analytics PRIVATE ofe_engine)

# ── NT8 bridge server (§AGT-06) ───────────────────────────────────────────────
# Full-pipeline: Coinbase feed → all analytics engines → TCP:7777 → NT8 footprint chart
# Protocol: 4-byte LE length-prefix + UTF-8 JSON
# Build: cmake --build build --target nt_bridge_server
# Run:   ./build/nt_bridge_server
#        OFE_NT_PORT=7778 ./build/nt_bridge_server
add_executable(nt_bridge_server
    tools/nt_bridge_server.cpp
)
target_compile_options(nt_bridge_server PRIVATE ${OFE_COMPILE_FLAGS})
target_link_options(nt_bridge_server PRIVATE ${OFE_LINK_FLAGS})
target_link_libraries(nt_bridge_server PRIVATE ofe_engine)

# ── Synthetic tick generator (stress testing) ─────────────────────────────────
# add_executable(ofe_tick_gen
#     # test/tick_generator.cpp  (added by AGT-10)
# )
# target_link_libraries(ofe_tick_gen PRIVATE ofe_engine)

# ── Install ───────────────────────────────────────────────────────────────────
install(TARGETS ofe_engine
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib
    ARCHIVE DESTINATION lib
)
install(DIRECTORY include/ DESTINATION include/ofe)

# ── Print config summary ──────────────────────────────────────────────────────
message(STATUS "==========================================================")
message(STATUS "  Order Flow Engine v${PROJECT_VERSION}")
message(STATUS "  Build type:    ${CMAKE_BUILD_TYPE}")
message(STATUS "  C++ standard:  ${CMAKE_CXX_STANDARD}")
message(STATUS "  Install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "==========================================================")
